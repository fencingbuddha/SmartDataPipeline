DATABASE_URL=sqlite:///./smartdata.db

# App settings
SMARTDATA_ENV=demo
SMARTDATA_DEBUG=false

# CORS/frontend origin (update if your frontend uses a different port)
FRONTEND_ORIGIN=http://localhost:5137

# Logging / security knobs (adjust to match your existing config)
LOG_LEVEL=info
ALLOW_ORIGINS=http://localhost:5137
